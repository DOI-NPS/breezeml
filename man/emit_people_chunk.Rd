% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_people.R
\name{emit_people_chunk}
\alias{emit_people_chunk}
\title{Emit the personnel.txt-writing chunk. Pure function - EMLassemblyline's
template_core_metadata() writes a BLANK personnel.txt; this chunk
overwrites it with the app's captured data, same pattern as
emit_fields_chunk(). Editors are intentionally excluded - not an EML role.}
\usage{
emit_people_chunk(state, working_folder_var = "working_folder")
}
\arguments{
\item{state}{the list returned by peopleServer()'s reactive, evaluated
(i.e. state <- people_reactive())}

\item{working_folder_var}{name of the R variable holding the working
folder path in the generated script (default "working_folder")}
}
\value{
character - the R code chunk to write personnel.txt, or an
explanatory comment if state is incomplete/invalid
}
\description{
Emit the personnel.txt-writing chunk. Pure function - EMLassemblyline's
template_core_metadata() writes a BLANK personnel.txt; this chunk
overwrites it with the app's captured data, same pattern as
emit_fields_chunk(). Editors are intentionally excluded - not an EML role.
}
