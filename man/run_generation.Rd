% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09_generate.R
\name{run_generation}
\alias{run_generation}
\title{Actually execute the pipeline:
\enumerate{
\item create <package_name>/data_package and .../data_package_creation
\item copy uploaded data files into data_package_creation (EMLassemblyline's
working directory) AND into data_package (the deliverable)
\item write every .txt template into data_package_creation
\item save the human-readable generation script into data_package_creation
\item apply NPS-specific EMLeditor edits (permissions, org context, DOI)
\item call make_eml() / eml_validate() / write_eml(), writing the .xml into
data_package_creation
\item copy the final .xml into data_package
}}
\usage{
run_generation(
  parent_folder,
  high_level_state,
  people_state,
  tables_state,
  fields_state,
  geo_state,
  taxonomy_state,
  permissions_state,
  org_context_state,
  doi_state = NULL,
  create_new_doi = FALSE
)
}
\arguments{
\item{parent_folder}{existing, writable directory under which the
package folder will be created}

\item{high_level_state}{the list returned by highLevelServer()'s reactive}

\item{people_state}{the list returned by peopleServer()'s reactive}

\item{tables_state}{the list returned by tableMetadataServer()'s reactive}

\item{fields_state}{the list returned by fieldsServer()'s reactive}

\item{geo_state}{the list returned by geographyServer()'s reactive}

\item{taxonomy_state}{the list returned by taxonomyServer()'s reactive}

\item{permissions_state}{the list returned by permissionsServer()'s reactive}

\item{org_context_state}{the list returned by org_contextServer()'s reactive}

\item{doi_state}{NULL, or list(reference_id=, doi=) from a prior
create-DOI step this session}

\item{create_new_doi}{logical - if TRUE, calls
EMLeditor::set_datastore_doi() to create a brand-new DataStore draft
reference (which in one call: creates the draft, attaches its DOI to
the EML object, AND updates every data table's onlineURL to the
draft's landing page). If an old doi_state is passed in, the
superseded draft is deleted afterward via
NPSdatastore::delete_inactive_ref(). If FALSE and doi_state is
non-NULL, calls EMLeditor::set_doi() instead, which re-attaches the
EXISTING DOI/URLs without creating a new draft - this is what happens
on every regenerate after a draft has already been created once,
avoiding the redundant/duplicate work of calling both functions.}
}
\value{
list(success = logical, message = character,
xml_path = character or NULL (final location in data_package),
validation_errors = character vector or NULL,
doi_state = list(reference_id=, doi=) or NULL - the
resulting DOI state after this call, for the caller to
persist and pass back in on the next generate)
}
\description{
Actually execute the pipeline:
\enumerate{
\item create <package_name>/data_package and .../data_package_creation
\item copy uploaded data files into data_package_creation (EMLassemblyline's
working directory) AND into data_package (the deliverable)
\item write every .txt template into data_package_creation
\item save the human-readable generation script into data_package_creation
\item apply NPS-specific EMLeditor edits (permissions, org context, DOI)
\item call make_eml() / eml_validate() / write_eml(), writing the .xml into
data_package_creation
\item copy the final .xml into data_package
}
}
