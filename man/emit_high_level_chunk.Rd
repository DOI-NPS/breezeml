% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_high_level_info.R
\name{emit_high_level_chunk}
\alias{emit_high_level_chunk}
\title{Emit the chunk that writes abstract.txt, methods.txt, keywords.txt, and
additional_info.txt, overwriting the blank templates that
EMLassemblyline::template_core_metadata() produces - same pattern as
emit_fields_chunk() / emit_people_chunk(). Pure function.}
\usage{
emit_high_level_chunk(state, working_folder_var = "working_folder")
}
\arguments{
\item{state}{the list returned by highLevelServer()'s reactive,
evaluated (i.e. state <- high_level_reactive())}

\item{working_folder_var}{name of the R variable holding the working
folder path in the generated script (default "working_folder")}
}
\value{
character - the R code chunk to write these .txt files, or an
explanatory comment if state is incomplete/invalid
}
\description{
Emit the chunk that writes abstract.txt, methods.txt, keywords.txt, and
additional_info.txt, overwriting the blank templates that
EMLassemblyline::template_core_metadata() produces - same pattern as
emit_fields_chunk() / emit_people_chunk(). Pure function.
}
