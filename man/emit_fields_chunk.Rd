% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_fields.R
\name{emit_fields_chunk}
\alias{emit_fields_chunk}
\title{Emit the R code chunks for attributes + categorical variables. Pure
function - writes the .txt template files' \emph{content} is actually produced
by EMLassemblyline itself (write.file = TRUE writes blank templates), so
what we emit here is:
\enumerate{
\item the template_table_attributes()/template_categorical_variables() calls
(matching skeleton.Rmd), AND
\item a post-processing chunk that programmatically overwrites those blank
templates with the user's actual entries from the app - this is what
lets the app's captured metadata flow into the files EMLassemblyline
expects, without requiring the user to re-type everything in Excel.
}}
\usage{
emit_fields_chunk(state, working_folder_var = "working_folder")
}
\arguments{
\item{state}{named list as returned by fieldsServer(), i.e.
list(<file_name> = list(attributes = tibble, catvars = tibble))}

\item{working_folder_var}{name of the working folder variable in-script}
}
\description{
Emit the R code chunks for attributes + categorical variables. Pure
function - writes the .txt template files' \emph{content} is actually produced
by EMLassemblyline itself (write.file = TRUE writes blank templates), so
what we emit here is:
\enumerate{
\item the template_table_attributes()/template_categorical_variables() calls
(matching skeleton.Rmd), AND
\item a post-processing chunk that programmatically overwrites those blank
templates with the user's actual entries from the app - this is what
lets the app's captured metadata flow into the files EMLassemblyline
expects, without requiring the user to re-type everything in Excel.
}
}
